To setup:

Run `jekyll bundle exec serve`
    - add --livereload for live reload

When switching from different OS's:
    - Modify `Gemfile.lock`
    - Go to PLATFORMS
        - x64-mingw-ucrt for Windows
        - x64-unknown for MacOS